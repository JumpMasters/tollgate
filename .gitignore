# Byte-compiled / cache
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/

# Build / distribution
build/
dist/
*.egg-info/
.eggs/

# Test, coverage, and type-check artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Local environment configuration (never commit secrets; use a template)
.env
*.env
!*.example.env

# Internal working docs — keep ADRs (docs/adr), ignore everything else
docs/superpowers/

# Assistant / Claude Code files — never committed
.claude/
CLAUDE.md
memory/

# OS and editor cruft
.DS_Store
*.swp
*~
