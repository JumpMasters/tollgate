"""Pure domain core: types, the cost model, and the reservation state machine.

This package is a leaf — it imports nothing from the rest of Tollgate. The
import-linter contract in ``pyproject.toml`` enforces that.
"""
